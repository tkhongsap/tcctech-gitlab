# GitLab API Configuration
# Replace with your actual values and rename this file to .env

# GitLab instance URL (without trailing slash)
GITLAB_URL=https://gitlab.example.com

# GitLab API token with appropriate permissions
# You can generate a token in GitLab: Profile > Settings > Access Tokens
# Required scopes: api, read_api, read_repository, write_repository
GITLAB_API_TOKEN=your_gitlab_api_token_here